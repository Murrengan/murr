;(function($) {
  let $name = 'murrenProfile',
      methods = {
        init: function() {
          return this.each(function() {
            let $this = $(this);

          })
        },
        follow: function() {
        }
      };
  $.fn[$name] = $.namespace(methods)
})(jQuery);