MurrenGameData:

        # Основные данные
        -- User (ForeignKey) get_user_model() - подтягиваем данные зарегистрированнаго клиента
        -- Уровень (IntegerField, default=9)
        -- Опыт (IntegerField)
        -- Текущая локация (CharField)
        -- HP (IntegerField)
        -- MP (IntegerField)

        # Характеристики
        -- Сила (IntegerField, default=5)
            -- Влияет на:
                -- Урон при физической атаке (WeaponAtk + Str + Bonus)
        -- Живучесть (IntegerField, default=5)
            -- Влияет на:
                -- Кол-во здоровья ( lvl * 10 + vit * 10 )
                -- Эффективность лечения предметами (Heal + Heal * Vit / 100)
                -- Снижение урона при его получении (DMG - DMG * Vit / 300 - DMG * ClothDef / 400 - Bonus) #TODO доработать
        -- Ловкость (IntegerField, max = 99, default=9)
            -- Влияет на:
                -- Шанс уворота #TODO Механику
                -- Точность #TODO Механику
        -- Интеллект (IntegerField, max = 99, default=9)
            -- Влияет на:
                -- Урон при магической атаке (WeaponAtk + Int + Bonus)
                -- Количество маны ( 100 + lvl * 10 + int * 3 )
        -- Удача (IntegerField, max = 99, default=9)
            -- Влияет на:
                -- Шанс уворота #TODO Механику
                -- Шанс нанести критический урон #TODO Механику
                -- Шанс найти что-то #TODO Механику

        # Одежда
        -- Оружие (FK поле)
        -- Голова (FK поле)
        -- Тело (FK поле)
        -- Руки (FK поле)
        -- Ноги (FK поле)
        -- Аксесуар (FK поле)
        -- Аксесуар (FK поле)

        # Инвентарь #TODO Че-то тут не так, M2M надо через другую модель делать будет, напрямую не указать количество
                    #TODO Возможно надо создать еще модель ПредметИнвентаря, из нее FK на игрока, FK на предмет и кол-во
                    #TODO И вроде норм через инлайны в админку прикрутить
        -- Инвентарь (M2M поле)
            -- Вешь - кол-во
            ...
            -- Вешь - кол-во


        # Скиллы #TODO Че-то тут не так, M2M надо через другую модель делать будет, напрямую не указать уровень
                 #TODO Возможно надо создать еще модель СкиллИгрока, из нее FK на игрока, FK на Скилл и уровень
                 #TODO И вроде норм через инлайны в админку прикрутить
        -- Скиллы (M2M поле)
            -- Скилл - уровень
            ...
            -- Скилл - уровень

    - @property поля
        -- Опыта до следующего уровня
        -- Сила физ атаки
        -- Сила маг атаки
        -- Точность
        -- Уворот
        #TODO Еще поля

Предмет одежды:
    - Поля БД
        -- Класс (CharField + Chooses)
        -- Название (CharField)
        -- Характеристика (IntegerField)

Предмет Используемый:
    - Поля БД
        -- Класс (CharField + Chooses)
        -- Название (CharField)
        -- Характеристика (IntegerField)

Предмет НЕ Используемый:
    - Поля БД
        -- Название (CharField)
        -- Вес (IntegerField)

Скилл:
    - Поля БД
        -- Название (CharField)
        -- Описание (TextField)
        -- Урон (IntegerField)
        -- Расход СП (IntegerField)
        -- Максимальный уровень (IntegerField)

Таблица опыта:
    - Поля БД
        -- Уровень
        -- Нобходимый опыт
